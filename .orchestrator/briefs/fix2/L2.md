task_id: fix2-L2
project_root: /home/user/RepOS
objective: Fix these independently verified review findings. Owner decisions to apply: keep HTTP 200 per-source degrade but the error string distinguishes query_error from unreachable, and query errors log at error level; inject now via createServer options (you own server.ts this run) and delete the module-global override, deriving generatedAt from the same instant; update today.test.ts to the new seam. Import the redaction helper from @repos/sources (the sources lane exports it first; if the export is not there when you start, wait for it by re-checking packages/sources/src/index.ts rather than duplicating the regex). Do not edit apps/api/src/index.ts (integrator) except that you may note the createServer call-site change in unresolved. F32 (500 detail) is yours: generic detail in production, detail only when NODE_ENV !== production.
  Findings:
  - F6 [minor] apps/api/src/services/sourceContext.ts:35: meeting_types ordered by `key`, a seed-only field; rep-created types (key NULL) always sort last, and sort_order/active are ignored entirely. FIX: ORDER BY sort_order, name and either filter inactive rows or expose `active` on MeetingTypeSummary.
  - F8b [major] apps/api/src/services/sourceContext.ts:31-37: NEW: readMeetingTypes casts raw rows to MeetingTypeSummary, so bigint id is emitted as a string while the shared contract declares id: number. Live payload violates the contract. FIX: Map the rows explicitly (id: Number(row.id), or keep id as a string in MeetingTypeSummary) so /api/sources matches packages/shared/src/api.ts.
  - F9,F10,F14 [major] apps/api/src/services/today.ts:80-102: One Promise.all under one try/catch: any block error blanks all five blocks, and a SQL typo or a pool-acquire timeout degrades a source with the same wording as a real outage. FIX: Settle the five block promises independently; classify pg codes (42703/42P01) and acquire timeouts separately from unreachable, log query errors at error level, keep HTTP 200 per-source degrade.
  - F12,F18 [major] apps/api/src/services/reachability.ts:24-31: No in-flight dedup or negative caching, so N concurrent requests fire N probes at a down source; and only 'unreachable' is ever rechecked, so a behind/unknown recheck latches until restart. FIX: Memoize the in-flight recheck promise per source with a short cooldown (10-30s), and re-validate every status on a TTL so a recovered source is promoted back.
  - F13,F20,F22 [major] apps/api/src/services/health.ts:19-20, 41-47, 62: Health verdict ignores schemaStatus; latencyMs starts after the recheck/acquire; timezone and meetingTypes failures are swallowed into plausible defaults. FIX: Fold schemaStatus into the verdict (anything not ok/ahead is degraded), time from the start of the probe, and surface a null timezone or error field instead of DEFAULT_TIMEZONE/[].
  - F15 [major] apps/api/src/routes/today.ts:8-14, services/today.ts:128: No request-level or per-source deadline anywhere; statement_timeout bounds one statement, so a slow source multiplies it by the number of serial statements. FIX: Add a per-request deadline around buildToday and a per-source budget inside buildOne, degrading the source that overruns.
  - F21 [minor] apps/api/src/services/today.ts:25-33, 136: The nowOverride module global is guarded only by NODE_ENV==='production', which apps/api/src/index.ts never sets, so it is armed outside the Docker image; generatedAt ignores it. FIX: Per the owner decision, inject `now` via createServer options instead of a module global, and derive generatedAt from that same instant.
  - F27 [major] apps/api/src/services/redact.ts:13: Redaction matches only scheme://user:pass@host, so a DSN carrying ?password= or a keyword DSN reaches a 500 body or a log line unredacted. FIX: Also strip password=/PGPASSWORD= key-value pairs in query strings and keyword DSNs before returning the message.
  - F32 [minor] apps/api/src/server.ts:56-62: The 500 handler returns errorMessage(err) as `detail` to unauthenticated callers; redaction strips only URL-shaped credentials, so hostnames, database names and relation names still reach the body. FIX: Log the detail server-side and return a generic detail, or include detail only when NODE_ENV !== 'production'.
owns:
  - apps/api/src/services/**
  - apps/api/src/routes/**
  - apps/api/src/server.ts
  - apps/api/src/degrade.test.ts
  - apps/api/src/today.test.ts
  - apps/api/src/sources.test.ts
  - apps/api/src/credentials-api.test.ts
may_read:
  - apps/api/src/**
  - packages/**
  - .env.test
verification_cmd: npm run typecheck && npm run lint && npm test -- apps/api/src/
return_schema: build_report
forbidden_returns: Do not return file contents, diffs, code excerpts over five lines, stack traces, test output, command transcripts or build logs. Report file:line pointers and exit codes only. Keep all free text under 1500 characters total.
unresolved: Fix exactly the findings listed. Touch only your owned files. Do not refactor anything you were not asked to fix, and do not weaken or delete a test to pass. A change you need outside your owned paths goes in unresolved as a precise one-line request. Do not run git commit.
isolation: Work only inside project_root. Never read /home/user/reptech-pathfinder. The local fixture databases (see .env.test) are available.
