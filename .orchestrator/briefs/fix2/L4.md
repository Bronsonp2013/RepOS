task_id: fix2-L4
project_root: /home/user/RepOS
objective: Fix these independently verified review findings. Owner decisions to apply: API base default is empty string so calls are same-origin /api/*; 60 s refetchInterval with 30 s staleTime and a visible as-of line from generatedAt; keep data on screen when a background refetch fails and show a banner. zod is hoisted at the workspace root, so import it; note in unresolved that apps/web/package.json should declare it (integrator). Keep every existing data-testid; add needs-visit-row-<accountId> testids. The e2e run starts the API and built preview via playwright.config.ts; keep it passing.
  Findings:
  - F24 [blocker] apps/web/src/lib/api.ts:8: The shipped bundle bakes an absolute base http://127.0.0.1:3200, which Caddyfile.example's connect-src 'self' blocks, and the Docker build cannot override it (no ARG, .env dockerignored). FIX: Default API_BASE_URL to '' so every call is same-origin /api/*, per the owner's Caddy decision.
  - F37,F38 [major] apps/web/src/main.tsx:15, pages/TodayPage.tsx:36, lib/api.ts:34: apiGet casts the response to T with no validation and nothing catches a render error, so any API shape drift becomes a blank white page instead of an error state. FIX: Parse responses with zod (already a dependency in packages/sources) in apiGet, and wrap App in an ErrorBoundary that renders a recoverable message.
  - F39 [major] apps/web/src/pages/TodayPage.tsx:17: isError is checked before data, so a failed background refetch discards good cached data and replaces the whole page with a raw error string. FIX: Render data whenever it is defined and show the error as a banner; fall back to the full-page error only when data is undefined.
  - F40,F41,F42 [major] apps/web/src/pages/TodayPage.tsx:19-36, components/CoverageBlock.tsx:22, PipelineBlock.tsx:22: No h1 anywhere (headings start at h2/h3), no role/aria-live on the loading and error regions, and progress bars are bare divs with an inline width only. FIX: Add an h1 ('Today' plus the as-of timestamp), role=status/aria-live on loading and role=alert on errors, and role=progressbar with aria-valuenow/min/max and a label on both bars.
  - F43 [major] apps/web/src/components/UpcomingTripsBlock.tsx:14,17; CoverageBlock.tsx:9; NeedsVisitBlock.tsx:15: Empty-state and metadata text uses text-slate-400 (#94a3b8) on white, about 2.6:1 at 14px, below the WCAG AA 4.5:1 minimum for body text. FIX: Use slate-600 or darker for empty-state and secondary text.
  - F44 [major] apps/web/src/pages/TodayPage.tsx:33-39: sources: [] renders four zero tiles and nothing else, so an empty or fully failed config looks identical to a healthy zero day, and generatedAt is never shown. FIX: Render an explicit 'No sources configured' state and show generatedAt as a visible 'as of' line in the header.
  - F45 [major] apps/web/src/main.tsx:9, pages/TodayPage.tsx:12: refetchOnWindowFocus is false with no staleTime, no refetchInterval and no refresh control, so an open Today tab never updates. FIX: Per the owner decision: 60s refetchInterval with 30s staleTime, plus a visible 'as of' timestamp in the header.
  - F23 [minor] apps/web/src/lib/api.ts:20-21, main.tsx:9: fetch is issued with no AbortSignal or timeout and the client retries once, so a slow /api/today hangs the page and doubles load on an already saturated 5-connection pool. FIX: Pass AbortSignal.timeout(...) into apiGet/fetchToday and drop retry for this query.
  - F47 [minor] apps/web/src/components/NeedsVisitBlock.tsx:35, e2e/today.spec.ts:25-29: Up to 8 links per source share the accessible name 'Open in Pathfinder' (WCAG 2.4.4), and the e2e test selects it with .first(), so it pins row ordering rather than the intended row. FIX: Give each link aria-label={`Open ${row.name} in Pathfinder`} and select the e2e link inside the needs-visit-row-48 testid.
  - F49,F62 [minor] apps/web/src/lib/api.ts:37-43, vite.config.ts:9, apps/web/src (no test): fetchSources/fetchHealth and queryKeys.sources/health have no consumers, the @web alias is unreferenced, and no test covers the page-level empty-sources state. FIX: Delete the unused exports and the alias (or use it), and add a TodayPage test for sources: [] asserting the new empty-state element.
owns:
  - apps/web/src/**
  - e2e/today.spec.ts
may_read:
  - packages/shared/src/**
  - apps/web/**
  - playwright.config.ts
  - .env.test
verification_cmd: npm run typecheck && npm run lint && npm test -- apps/web && npm run build -w apps/web && npm run e2e
return_schema: build_report
forbidden_returns: Do not return file contents, diffs, code excerpts over five lines, stack traces, test output, command transcripts or build logs. Report file:line pointers and exit codes only. Keep all free text under 1500 characters total.
unresolved: Fix exactly the findings listed. Touch only your owned files. Do not refactor anything you were not asked to fix, and do not weaken or delete a test to pass. A change you need outside your owned paths goes in unresolved as a precise one-line request. Do not run git commit.
isolation: Work only inside project_root. Never read /home/user/reptech-pathfinder. The local fixture databases (see .env.test) are available.
