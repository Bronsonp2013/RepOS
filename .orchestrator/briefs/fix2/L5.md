task_id: fix2-L5
project_root: /home/user/RepOS
objective: Fix these independently verified review findings. Owner decisions to apply: same-origin deploy via Caddy (container binds 0.0.0.0 via compose environment only, host publish stays 127.0.0.1, both sides of the port mapping use ${REPOS_API_PORT}); the web bundle is served by Caddy from a volume the api image populates (choose the simplest correct mechanism and document it); REPOS_V1 sections 2.3, 4, 7, 8 synced to the built API in packages/shared/src/api.ts and the nine criteria in .orchestrator/state.md; document tripsThisWeek overlap semantics; document default_transaction_read_only as defense in depth. No Docker daemon here: verify by careful read-through and say so. Do not edit .env.example (integrator owns it); put the REPOS_WEB_ORIGIN default change in unresolved.
  Findings:
  - F25,F50 [blocker] docker-compose.yml:25, .env.example:17: env_file supplies REPOS_API_HOST=127.0.0.1, so the API binds container loopback while Docker publishes to the container's bridge IP; the mapped port is refused and Caddy never reaches the API. FIX: Per the owner decision, set REPOS_API_HOST=0.0.0.0 in the compose service environment only; the 127.0.0.1: host-side publish still restricts exposure.
  - F52 [blocker] docker-compose.yml:25: The published mapping is '127.0.0.1:${REPOS_API_PORT:-3200}:3200' — the container side is hardcoded while the app listens on REPOS_API_PORT, and DEPLOY.md:62 invites changing that port. FIX: Use '127.0.0.1:${REPOS_API_PORT:-3200}:${REPOS_API_PORT:-3200}' so both sides move together.
  - F53,F54 [major] docker-compose.yml:1-27, Caddyfile.example:19, docs/DEPLOY.md:77: The docs tell Caddy to serve apps/web/dist from the host, but the bundle exists only inside the image and nothing extracts it; the vite-preview fallback names a 'web' service and port 5173 that do not exist. FIX: Add a named volume or bind mount for apps/web/dist (or document a docker cp / host build step), and either add a real web service with a published port or delete the fallback from both comment blocks.
  - F34,F55 [major] docs/DEPLOY.md:58-65: First-run step 2 never mentions REPOS_WEB_ORIGIN although the image sets NODE_ENV=production and the server throws at boot without it, so step 3 always fails. FIX: Add REPOS_WEB_ORIGIN=https://repos.<tailnet>.ts.net to the DEPLOY.md fill-in list and correct .env.example's loopback default (INT owns .env.example).
  - F56,F57,F61 [major] docs/REPOS_V1.md §2.3, §4, §7, §8; README.md:52: §7 lists eight criteria while README and the tests say nine; §2.3 says a sources.json entry carries a read-only DATABASE_URL, contradicting §6, CLAUDE.md and the code; §4's field lists and §8's sources.example.json are stale. FIX: Sync §2.3 (slug/name/kind/webUrl, credential in .env), regenerate §4's lists from packages/shared/src/api.ts, add the ninth criterion from .orchestrator/state.md, and replace sources.example.json with sources.json.
  - F58 [major] README.md:54-55, docs/DEPLOY.md: README points at DEPLOY.md for the fixture databases, but DEPLOY.md never mentions them; the real setup lives in test/fixtures/pathfinder/LOCAL_DB.md, which is excluded from the repo. FIX: Add a fixture/test-database section to DEPLOY.md (or point README at test/fixtures/pathfinder/README.md) and commit a non-secret LOCAL_DB template.
  - F3,F29 [minor] docs/REPOS_V1.md §3-§4 (totals.ts:51-55 vs upcomingTrips.ts:51, pool.ts:10): Two documented guarantees overstate the code: the 'Trips this week' tile is week-overlap any-status while the list is start_date >= today, and default_transaction_read_only is a session default, not a lock. FIX: Per the owner decision, keep both behaviours and document them: state the two trip definitions in §3/§4, and describe the read-only session option as defense-in-depth behind the repos_reader grant.
  - F30,F31 [minor] docs/DEPLOY.md:31-37: The grant recipe never revokes TEMP from PUBLIC, so repos_reader can CREATE TEMP TABLE, and ALTER DEFAULT PRIVILEGES silently grants SELECT on every future Pathfinder table, including one that may hold raw secrets. FIX: Add REVOKE TEMP ON DATABASE <db> FROM PUBLIC, repos_reader, and either drop the default-privileges line in favour of per-table grants or gate it on the schema-coupling bump.
  - F48 [minor] Caddyfile.example:19-20: file_server without try_files makes any deep link 404 before React loads, so App.tsx's catch-all redirect route is dead in the documented deploy. FIX: Add `try_files {path} /index.html` to the static block (or reverse_proxy the preview server instead).
owns:
  - Dockerfile
  - docker-compose.yml
  - Caddyfile.example
  - docs/**
  - README.md
  - test/fixtures/pathfinder/README.md
  - test/fixtures/pathfinder/LOCAL_DB.example.md
may_read:
  - the whole project root
verification_cmd: npm run typecheck && npm run lint
return_schema: build_report
forbidden_returns: Do not return file contents, diffs, code excerpts over five lines, stack traces, test output, command transcripts or build logs. Report file:line pointers and exit codes only. Keep all free text under 1500 characters total.
unresolved: Fix exactly the findings listed. Touch only your owned files. Do not refactor anything you were not asked to fix, and do not weaken or delete a test to pass. A change you need outside your owned paths goes in unresolved as a precise one-line request. Do not run git commit.
isolation: Work only inside project_root. Never read /home/user/reptech-pathfinder. The local fixture databases (see .env.test) are available.
