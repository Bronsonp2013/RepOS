task_id: fix2-L1
project_root: /home/user/RepOS
objective: Fix these independently verified review findings. Blocks must take everything they need from BlockContext (now, timezone); the services lane is concurrently replacing the module-global clock override, so do not depend on it. Extend seed-graham.sql idempotently (explicit ids + setval like existing rows) and re-apply it to repos_test_lexington via REPOS_TEST_SUPERUSER_DATABASE_URL in .env.test; account 48 must stay first in needsVisit and the venture DB stays empty. Owner decision: keep tripsThisWeek overlap semantics; document in the contract comment only if you own it (you do not own packages/shared; put a one-line note in unresolved). Keep ids as strings only if the shared contract already allows it; otherwise build hrefs from the raw string id and note the contract change in unresolved.
  Findings:
  - F1,F2,F4,F7 [minor] blocks/coverage.ts:53, upcomingTrips.ts:73, totals.ts:46-60, needsVisit.ts:46: UNTESTED, not proven bugs: weekly/custom period_key, soft-deleted-account stops, unstaged prospects, stale-vs-active stage mismatch. Fixture has no row exercising any of them. FIX: Add fixture rows (weekly+custom cycle, soft-deleted account with a stop, soft-deleted prospect_stage, non-active-stage never-visited account) and assert the intended counts before changing SQL.
  - F8 [minor] apps/api/src/blocks/needsVisit.ts:52, coverage.ts:85, upcomingTrips.ts:109: bigint ids go through Number() and round above 2^53, so a deep-link href built from the rounded value would 404 while the raw string id was in hand. FIX: Keep ids as strings in the contract and build hrefs from the raw string (upcomingTrips.ts:116 already does).
  - F15b [major] apps/api/src/blocks/coverage.ts:47-55: coverage runs 1+N serial queries (one per active cycle); statement_timeout is per statement, so worst-case latency scales with cycle count and no bound caps the block. FIX: Collapse the per-cycle loop into one set-based query joining cycles to the eligible/covered subqueries, with period_key passed as an array or computed per row.
owns:
  - apps/api/src/blocks/*.ts (not index.ts)
  - apps/api/src/blocks/today-blocks.test.ts
  - test/fixtures/pathfinder/seed-graham.sql
may_read:
  - apps/api/src/**
  - packages/**
  - test/fixtures/pathfinder/**
  - .env.test
verification_cmd: npm run typecheck && npm run lint && npm test -- blocks/
return_schema: build_report
forbidden_returns: Do not return file contents, diffs, code excerpts over five lines, stack traces, test output, command transcripts or build logs. Report file:line pointers and exit codes only. Keep all free text under 1500 characters total.
unresolved: Fix exactly the findings listed. Touch only your owned files. Do not refactor anything you were not asked to fix, and do not weaken or delete a test to pass. A change you need outside your owned paths goes in unresolved as a precise one-line request. Do not run git commit.
isolation: Work only inside project_root. Never read /home/user/reptech-pathfinder. The local fixture databases (see .env.test) are available.
